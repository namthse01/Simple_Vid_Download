# Engine tải về (rất nặng: ffmpeg/ffprobe ~145MB mỗi file, vượt giới hạn 100MB của GitHub)
# Chạy setup.ps1 để tải lại.
bin/

# Hồ sơ trình duyệt nhúng — CHỨA COOKIE ĐĂNG NHẬP, tuyệt đối không đưa lên git
wvdata/

# Log mỗi lần tải (chứa link video + token có chữ ký)
logs/

# Cấu hình riêng của máy (thư mục lưu, chất lượng đã chọn)
settings.json

# Editor
.vscode/
