<Window x:Class="SimpleVidDownload.ResolveWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Đang lấy link video thật..." Height="640" Width="920"
        WindowStartupLocation="CenterScreen" Background="{StaticResource Bg}"
        Foreground="{StaticResource Text}" FontSize="13" Icon="appicon.ico"
        FontFamily="Segoe UI" TextOptions.TextFormattingMode="Display">

  <Grid Margin="16,14,16,16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Border Grid.Row="0" Background="{StaticResource Card}" CornerRadius="10"
            Padding="14,12" Margin="0,0,0,12">
      <StackPanel Orientation="Horizontal">
        <TextBlock Text="⏳" FontSize="17" VerticalAlignment="Center" Margin="0,0,12,0"/>
        <TextBlock x:Name="LblStatus" Foreground="{StaticResource Warn}" FontSize="13"
                   TextWrapping="Wrap" VerticalAlignment="Center"
                   Text="Đang mở player để lấy link video thật. Chờ vài giây, cửa sổ này tự đóng..."/>
      </StackPanel>
    </Border>

    <Border Grid.Row="1" Background="#000000" CornerRadius="10"
            BorderBrush="{StaticResource Line}" BorderThickness="1">
      <Border CornerRadius="9" ClipToBounds="True" Background="#000000">
        <wv2:WebView2 x:Name="Web"/>
      </Border>
    </Border>
  </Grid>
</Window>
